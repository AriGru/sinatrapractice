source "https://rubygems.org" #tell them I am using ruby gems
ruby "2.2.2" #need to specify what version I am using

gem "sinatra" # listing what gems I would like to use/showimg the ruby part of my 
#then type in bundle install - it shows that the folder is working correctly

##must tell what version we are using so they updates works correctly
#gem file lock puts the version number so we get the specific version we need

#dynamic website is when is changes everythime you refresh etc anything you can log into
#static website - does not change when you refresh it 